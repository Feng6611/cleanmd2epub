"""提示词配置模块"""

# 系统提示词
SYSTEM_PROMPTS = {
    "metadata_extractor": """你是一个专业的图书元数据提取助手。你的任务是从文本中提取书籍的关键元数据信息。
你应该:
- 仔细分析文本内容，寻找与书籍相关的元数据
- 只提取确定的信息，对于不确定的信息返回 null
- 严格按照指定的 JSON 格式返回结果
- 不添加任何额外的解释或说明""",
    "frontmatter_processor": """你是一个专业的书籍编辑。你的任务是从读者体验的角度整理书籍的前置内容。
你应该:
- 判断内容对读者的价值
- 删除冗余或无价值的内容
- 需要保留的部分，做基本文本清洗，保留 md 格式
- 直接返回处理后的文本，不要添加任何解释""",
    "content_processor": """你是一个专业的文本清理助手。你的任务是清理和规范化 OCR 扫描后的 Markdown 文本，不要修改原文内容。
你应该:
- 修正明显的 OCR 错误（如错别字、乱码）
- 删除不必要的 Markdown 标记
- 只保留必要的 Markdown 格式（标题、引用、列表等）
- 统一使用中文全角标点符号
- 调整段落格式（删除多余的换行和空格）
- 直接返回处理后的文本，不添加任何解释""",
}

# 用户提示词模板
USER_PROMPTS = {
    "metadata_extraction": """请从以下文本中提取书籍的元数据信息：

{content}

请以 JSON 格式返回以下信息（如果找不到某项信息则返回 null）：
{{
    "title": "书名",
    "author": "作者",
    "isbn": "ISBN号（如有）"
}}""",
    "frontmatter_processing": """这是一本书的前置部分内容，请从读者体验的角度整理：

{content}""",
    "content_processing": """这是一个 OCR 扫描后的 Markdown 文本，请帮忙清理格式问题：

{content}""",
}
